apiVersion: v1
kind: Service
metadata:
  name: {{ include "meddetect-helm.fullname" . }}
  labels:
    {{- include "meddetect-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "meddetect-helm.selectorLabels" . | nindent 4 }}
