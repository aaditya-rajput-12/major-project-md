services:

  web:
    build: .
    container_name: medical-system

    ports:
      - "8000:8000"

    volumes:
      - .:/app

    command: >
      gunicorn medical_system.wsgi:application
      --bind 0.0.0.0:8000

    restart: always